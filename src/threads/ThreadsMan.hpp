#pragma once

class ThreadsMan {
   private:
    /* data */
   public:
    /**
     * Fuction server as the bcackground thread function,
     * invokes other functions
     * */
    static void ThreadManager();
};